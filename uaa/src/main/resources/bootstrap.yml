spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username:
    password:
    hikari:
      connection-timeout: 20000
      maximum-pool-size: 5

  jpa:
    hibernate:
      ddl-auto: create